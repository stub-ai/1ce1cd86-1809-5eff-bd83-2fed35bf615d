using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    public bool canMove = true;

    // Update is called once per frame
    void Update()
    {
        if (canMove)
        {
            // Add your player movement code here
        }
    }
}